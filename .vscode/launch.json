{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Bun with Args",
            "program": "${workspaceFolder}/compass/index.ts",
            "args": [
                "--repo",
                "/tmp/test/",
                 "--fixture",
                 "../ignore/OLD/Compass/config/sample-config.json",
                 "--model",
                 "gpt-4.1",
                 "--eval-model",
                 "gpt-4.1",
                 "--agent-type",
                 "GitHubCopilot"
            ], // Arguments for your script
            "cwd": "${workspaceFolder}/compass/",
            "runtimeArgs": [], // Arguments for the 'bun' runtime itself
            "stopOnEntry": false
        }
    ]
}