{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Bun with Args",
            "program": "${workspaceFolder}/src/index.ts",
            "args": [
                "--repo",
                ".",
                 "--fixture",
                 "${workspaceFolder}/src/fixture.json",
                 "--model",
                 "gpt-4.1",
                 "--eval-model",
                 "gpt-4.1",
                 "--agent-type",
                 "GitHubCopilot"
            ], // Arguments for your script
            "cwd": "${workspaceFolder}",
            "runtimeArgs": [], // Arguments for the 'bun' runtime itself
            "stopOnEntry": false
        }
    ]
}